import BalanceChecker from '../../components/(balancechecker)/BC';

const CheckICOBalance: React.FC = () => {
    return (
        <div>
            <h1>Welcome to the Contract Balance Checker</h1>
            <BalanceChecker />
        </div>
    );
};

export default CheckICOBalance;
